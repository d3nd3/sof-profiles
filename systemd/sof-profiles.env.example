# Copy to /etc/sof-profiles/env and edit.

# Required for userinfo-rcon.service
SOF_PROFILES=/path/to/sof-profiles
RCON_PASSWORD=
# EXPORT_FIRE_HOST=127.0.0.1
# EXPORT_FIRE_PORT=8765
# RCON_HOST=127.0.0.1
# RCON_PORT=0
# VERBOSE=1

# Only needed when installing export-fire.service (skip with install.sh --rcon-only)
# SOF_USER_ROOT=/path/to/sof-server/user-28921
# SOF_EXPORT_FIRE=/path/to/sof-export-fire/sof_export_fire.py
